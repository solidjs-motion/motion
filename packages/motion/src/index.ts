// Phase 1 — public API surface. Implementations land incrementally; this file
// re-exports each piece as it's wired up.

export type * from "./types"

// Placeholder kept so examples that import the package still resolve while
// Phase 1 implementation is in flight. Removed once useMotion lands.
export const placeholder = true
